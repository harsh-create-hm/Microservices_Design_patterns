server:
  port: 8080

spring:
  application:
    name: client-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
