server:
  port: 8086

spring:
  application:
    name: client-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
